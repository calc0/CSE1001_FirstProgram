public class IntOps {
    public static vois main(Int[] args) {
        int a, b, sum, prod, quot, rem;
        sum = args[0] + args[1];
        prod = args[0] * args[1];
        quot = args[0] / args[1];
        rem = args[0] 
        
    }
}
